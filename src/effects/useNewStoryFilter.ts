import { State } from "../state";
import { HNStory } from "../types";

export function useNewStoryFilter(storyRecord: any) {
  //   const topStoriesOrderedList = ids
  //     .map((id) => storyRecord[id])
  //     .sort((a, b) => {
  //       return a.time > b.time ? -1 : 1;
  //     }) as HNStory[];
  //   return topStoriesOrderedList;
}
